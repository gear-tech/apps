[workspace]
resolver = "2"

members = [
    "fungible-token",
    "gear-feeds-router",
    "ping",
    "fungible-token-messages",
    "non-fungible-token",
]

[profile.release]
lto = true
opt-level = "s"
